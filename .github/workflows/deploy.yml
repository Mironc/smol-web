name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Deploy
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
